{
  "name": "preflight-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "workers/*",
    "mcp"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspace=mcp",
    "start": "npm run start --workspace=mcp & npm run start --workspace=@preflight/worker-evm & wait",
    "dev:mcp": "npm run dev --workspace=mcp",
    "dev:worker": "npm run dev --workspace=@preflight/worker-evm",
    "lint": "npm run lint --workspace=mcp"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}